export interface PillProps { // to adjust the pill component to accept the prop title
  title: string
}

export interface ButtonProps { //defines the type of props that button component expects.
  title: string; 
  styles?: string; // optional styles string for custom classes
}
